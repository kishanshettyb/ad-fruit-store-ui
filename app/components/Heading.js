import { View, Text } from "react-native";
import React from "react";

const Heading = () => {
	return (
		<View className="py-[40]">
			<Text className="text-5xl w-[200] font-bold">Deliver to Home</Text>
		</View>
	);
};

export default Heading;
